FROM node:22-slim

WORKDIR /app

RUN npm install -g openclaw

ENV NODE_ENV=production
ENV PORT=8080

EXPOSE 8080

CMD ["node", "--experimental-vm-modules", "/usr/local/lib/node_modules/openclaw/openclaw.mjs", "gateway", "--allow-unconfigured"]
